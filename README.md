# sapient-node
Nextjs backend deployment

# Dependencies

> npm install

# Start server

> npm start


# Graphql query
{
  name {
      name,
      address1,
      address2,
      city,
      country,
      zip_code,
      email,
      tel
  }
}
